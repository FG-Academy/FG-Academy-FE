# Auth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-key

# Server API URL (for server-side rendering)
SERVER_API_URL=http://localhost:8080

# Image URL (legacy - for backward compatibility with old local images)
# New images are stored in S3 and URLs are returned directly from the API
NEXT_PUBLIC_IMAGE_URL=http://localhost:8080
